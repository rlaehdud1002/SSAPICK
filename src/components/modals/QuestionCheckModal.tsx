import { DialogDescription } from '@radix-ui/react-dialog';

const QuestionCheckModal = () => {
  return (
    <DialogDescription className="flex justify-center">
      <h1 className="my-12">질문 생성 신청이 완료되었습니다.</h1>
    </DialogDescription>
  );
};

export default QuestionCheckModal;
